# apps/web — owner P3

React editor: upload, Remotion Player, transcript panel, word inspector, preset picker,
undo, mic button, agent step log. Build against the fixture first.
Init with: `npm create vite@latest . -- --template react-ts`, then Tailwind + shadcn/ui.
