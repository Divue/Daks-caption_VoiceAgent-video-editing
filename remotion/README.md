# remotion — owner P2

Caption composition rendering a Project over the video. Start from
`packages/shared/fixtures/demo-project.json` and `packages/shared/src/presets.ts`.
Export via Remotion Lambda; fallback `@remotion/renderer` in the backend container.
Init with: `npx create-video@latest` (blank template), then import `@captions/shared`.
